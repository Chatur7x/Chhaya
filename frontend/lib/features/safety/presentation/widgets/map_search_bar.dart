import 'package:flutter/material.dart';import 'package:flutter/services.dart';import 'dart:ui';import '../../../../core/theme/catus_theme.dart';/// Premium search bar — Apple Maps + Google Maps hybrid styleclass MapSearchBar extends StatefulWidget {  final int batteryLevel;  final String channelType;  final TextEditingController? controller;  final VoidCallback? onTap;  const MapSearchBar({    super.key,    required this.batteryLevel,    this.channelType = 'ble',    this.controller,    this.onTap,  });  @override  State<MapSearchBar> createState() => _MapSearchBarState();}class _MapSearchBarState extends State<MapSearchBar> {  bool _focused = false;  @override  Widget build(BuildContext context) {    return ClipRRect(      borderRadius: BorderRadius.circular(18),      child: BackdropFilter(        filter: ImageFilter.blur(sigmaX: 30, sigmaY: 30),        child: AnimatedContainer(          duration: const Duration(milliseconds: 200),          height: 52,          padding: const EdgeInsets.symmetric(horizontal: 16),          decoration: BoxDecoration(            color: CatusTheme.surface.withValues(alpha: 0.85),            borderRadius: BorderRadius.circular(18),            border: Border.all(              color: _focused ? CatusTheme.accent.withValues(alpha: 0.5) : Colors.white.withValues(alpha: 0.06),              width: _focused ? 1.5 : 0.5,            ),            boxShadow: [              BoxShadow(                color: Colors.black.withValues(alpha: 0.3),                blurRadius: 20,                offset: const Offset(0, 6),              ),              if (_focused) BoxShadow(                color: CatusTheme.accent.withValues(alpha: 0.1),                blurRadius: 20,              ),            ],          ),          child: Row(children: [            Icon(Icons.search_rounded, color: _focused ? CatusTheme.accent : CatusTheme.textMuted, size: 22),            const SizedBox(width: 12),            Expanded(              child: Focus(                onFocusChange: (f) => setState(() => _focused = f),                child: TextField(                  controller: widget.controller,                  onTap: widget.onTap,                  style: const TextStyle(color: CatusTheme.textPrimary, fontSize: 15, fontWeight: FontWeight.w500),                  decoration: const InputDecoration(                    hintText: 'Search places, contacts, hazards...',                    hintStyle: TextStyle(color: CatusTheme.textMuted, fontSize: 14),                    border: InputBorder.none,                    enabledBorder: InputBorder.none,                    focusedBorder: InputBorder.none,                    contentPadding: EdgeInsets.zero,                    isDense: true,                  ),                ),              ),            ),            const SizedBox(width: 8),            CatusTheme.channelBadge(widget.channelType),            const SizedBox(width: 8),            _BatteryPill(level: widget.batteryLevel),          ]),        ),      ),    );  }}class _BatteryPill extends StatelessWidget {  final int level;  const _BatteryPill({required this.level});  @override  Widget build(BuildContext context) {    final isLow = level <= 20;    final isMid = level <= 50 && !isLow;    final color = isLow ? CatusTheme.sosRed : isMid ? CatusTheme.warningYellow : CatusTheme.safeGreen;    return Container(      padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 5),      decoration: BoxDecoration(        color: color.withValues(alpha: 0.12),        borderRadius: BorderRadius.circular(12),        border: Border.all(color: color.withValues(alpha: 0.3)),      ),      child: Row(mainAxisSize: MainAxisSize.min, children: [        Icon(          isLow ? Icons.battery_alert_rounded : isMid ? Icons.battery_4_bar_rounded : Icons.battery_full_rounded,          size: 14,          color: color,        ),        const SizedBox(width: 4),        Text('$level%',            style: TextStyle(fontSize: 12, fontWeight: FontWeight.w700, color: color)),      ]),    );  }}